clusters:
    dev:
        labels:
            tier: dev
            env: platform
    dev2:
        labels:
            tier: dev
            env: platform
            extra_label: foo
            extral_label2: bar
    staging:
        labels:
            tier: staging
            env: platform
    prod:
        labels:
            tier: prod
            env: platform
    argocd:
        labels:
            tier: production
            env: internal-services