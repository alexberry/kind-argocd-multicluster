# Overrides for cluster: dev
# This file is optional - clusters without an override file get base values only
env:
  CLUSTER_NAME: "dev"
  DEBUG: "true"
  LOG_LEVEL: "debug"

hpa:
  maxReplicas: 1

resources:
  requests:
    cpu: 50m
    memory: 64Mi
  limits:
    cpu: 100m
    memory: 128Mi
